import styled from "styled-components";

const TrustSection = () => {
  return (
    <SectionWrapper>
      <div></div>
    </SectionWrapper>
  );
};

// Styles
const SectionWrapper = styled.div``;

// Exports
export default TrustSection;
